{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "player:build": "pnpm --filter @workspace/player run build",
    "player:cap:sync": "pnpm --filter @workspace/player exec cap sync",
    "player:cap:sync:ios": "pnpm --filter @workspace/player exec cap sync ios",
    "player:cap:open:ios": "pnpm --filter @workspace/player exec cap open ios",
    "player:ios": "pnpm run player:build && pnpm run player:cap:sync:ios && pnpm run player:cap:open:ios"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
