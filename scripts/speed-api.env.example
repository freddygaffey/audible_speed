# Copy to e.g. /opt/speed/.env.speed or /etc/speed/api.env and point systemd EnvironmentFile= at it.
# Required:
PORT=3001

# Optional:
# NODE_ENV=production
# LOG_LEVEL=info
# AUDIBLE_WIDEVINE_KEYSERVICE_URL=https://<your-widevine-bridge>
#
# Temporary server artifact lifecycle (phone-first):
# SPEED_DOWNLOAD_DONE_TTL_MS=7200000
# SPEED_DOWNLOAD_SWEEP_INTERVAL_MS=60000
